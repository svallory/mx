{
  "marko": "divergence",
  "reason": "<fragment> as an explicit multi-root wrapper is MX-only syntax; Marko templates and tag bodies natively support multiple root nodes without a wrapper tag and reject an explicit <fragment> as an unresolvable custom tag."
}
