export interface Input { status: string }
<if=input.status === "active">
  <p>Active</p>
</if>
<else if=(input.status === "paused")>
  <p>Paused</p>
</else>
<else if=(input.status === "closed")>
  <p>Closed</p>
</else>
<else>
  <p>Unknown</p>
</else>
