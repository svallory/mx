{
  "marko": "divergence",
  "reason": "S3 'Output module shape' (notes/standalone-mx-decisions.md) settles MX's own convention: 'Components are imports called as functions returning string; attribute tags become named function props on the call' — input.header() is exactly that settled calling convention, not an open question on the mx-html side. Marko's own attribute-tag mechanism uses a different calling convention (not a plain callable prop), which this fixture's Marko translation hits directly — a genuine, understood toolchain difference between the two systems' component models, not a bug in either."
}
