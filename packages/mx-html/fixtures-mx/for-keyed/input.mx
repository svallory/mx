export interface Input { items: { id: string; name: string }[] }
<ul>
  <for|it, i| of=input.items by="id">
    <li>${it.id}: ${it.name}</li>
  </for>
</ul>
<ol>
  <for|it, i| of=input.items by=identity>
    <li>${it.name}</li>
  </for>
</ol>
