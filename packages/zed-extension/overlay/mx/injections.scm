; MX-specific injection overlay, concatenated onto upstream marko-js/tree-sitter's
; queries/injections.scm by scripts/vendor.sh. Empty for now: upstream's TS/CSS/HTML
; injections already cover every expression position MX uses (the grammar is
; unmodified — see notes/zed-extension-plan.md decision 3).
