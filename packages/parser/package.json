{
  "name": "@mxlang/parser",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "src/public.d.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "bun build src/index.ts --outdir dist --target node --format esm --external @mxlang/solid --define process.env.BABEL_8_BREAKING=false --define process.env.USE_ESM=true --define process.env.IS_PUBLISH=true"
  },
  "dependencies": {
    "@mxlang/solid": "workspace:*"
  }
}
