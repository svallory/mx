import type { CustomTag } from "../../../../custom-tags.ts";

/** The outer `tags/` directory: must lose to the nearer one below. */
const badge: CustomTag = { attributes: { where: { type: "string" } } };
export default badge;
