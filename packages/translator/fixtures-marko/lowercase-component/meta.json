{
  "marko": "error",
  "errorPattern": "unless they are PascalCase\\. Use `<\\$\\{layout\\}/>` or rename to `Layout`\\.",
  "reason": "real Marko rejects a lowercase local-variable tag reference outright (\"Local variables must be in a dynamic tag unless they are PascalCase. Use `<${layout}/>` or rename to `Layout`.\") -- verified against @marko/compiler 5.42.5 / marko@6.3.51. Fixed: @markox/translator's emitComponent now rejects the same reference with Marko's own wording. See fixtures dynamic-tag-lowercase-import and nested-layout for the two forms that still work: `<${layout}/>` and `<Layout/>`."
}
