{
  "name": "@markox/translator",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "example": "bun run --tsconfig-override=../../tsconfig.base.json src/example.ts"
  },
  "dependencies": {
    "@marko/compiler": "5.42.5",
    "@markox/html": "workspace:*",
    "@markox/parser": "workspace:*"
  }
}
