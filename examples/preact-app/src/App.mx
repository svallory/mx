import Counter from "./Counter.mx";

export interface Input {}

<main>
  <Counter label="MX counter" start=3/>
  <for|name, index| of=["alpha", "beta", "gamma"]>
    <p data-testid=`item-${index}`>${index}: ${name}</p>
  </for>
</main>
