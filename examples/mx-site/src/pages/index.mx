import Layout from "./layout.mx"
import Callout from "./partials/callout.marko"

export interface Input { }

<Layout title="Home" active="home">
  <Callout text="This whole site is MX (.mx) templates — one partial kept as .marko to show the alias — rendered server-side to a string — no client runtime." />
  <section.intro>
    <h2>@markox/translator demo</h2>
    <p>Use the nav above to see a component import, doctype/head/meta (this page), loops and empty-state handling, attribute escaping, mixins (<code>&lt;define&gt;</code>), and raw vs escaped output.</p>
  </section>
</Layout>
