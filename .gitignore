node_modules/
dist/
.gen/
coverage/
.moon/cache/
*.log

# editor
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store

# Zed's own dev-install scratch: it clones/compiles [grammars.*] repos here
# when "zed: install dev extension" is run against packages/zed-extension.
# Not our output, not meant to be committed.
packages/zed-extension/grammars/

# Agent probe scratch (oracle harness experiments)
.probe/

# verify-coverage evidence: deleted before each verify run, regenerated by
# `bun run test` / tree-sitter-solidmx's test.sh, read once, never committed.
/vitest-results.json
/packages/tree-sitter-solidmx/.test-ran
/.verify-start
.astro/

# docs site build output (apps/docs); the build is the verify check, the
# output is not committed
apps/docs/site/
